name: Build MagicControl

on:
  push:
    branches: [ main ]

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      - name: Build Android APK
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease
      - name: Find and Upload APK
        run: |
          # Chercher l'APK dans tous les dossiers possibles
          find app/build -name "*.apk" | head -10
          APK_PATH=$(find app/build -name "*.apk" | grep -v "unsigned" | head -1)
          if [ -n "$APK_PATH" ]; then
            echo "📦 APK trouvé: $APK_PATH"
            cp "$APK_PATH" magiccontrol-release.apk
            ls -la magiccontrol-release.apk
          else
            echo "❌ Aucun APK trouvé!"
            find app/build -type f -name "*.apk" || echo "Aucun fichier APK trouvé"
            exit 1
          fi
      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: magiccontrol-android
          path: magiccontrol-release.apk
